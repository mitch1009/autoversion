name: 'autoversion'
description: 'Automate Versions'
inputs:

  overide:  # id of input
    description: 'Who to greet'
    required: false
  apend: #add more digits
    descrioption: "add more digits  to the existing 3 digit version format"
    required: false

outputs:
  time: # id of output
    description: 'The time we greeted you'
  message: # 2nd id of output
    description: 'message'
runs:
  using: 'node16'
  main: 'index.js'
